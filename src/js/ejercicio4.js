const base = 10;
const altura = 6;

const area = (base, altura) => {
    console.log(`El área de un rectángulo es ${base * altura} cm²`);
}

const perimetro = (base, altura) => {
    console.log(`el perímetro de un rectángulo es ${2 * (base + altura)} cm`)
}
area (base, altura)
perimetro(base, altura)
